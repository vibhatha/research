FROM node:22-alpine

WORKDIR /app

# Install dependencies (cache layer)
COPY package.json package-lock.json* ./
RUN npm install

# Copy application code
COPY . .

EXPOSE 3000

# Run in dev mode
CMD ["npm", "run", "dev"]
